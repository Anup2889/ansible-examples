---
- name: Install WordPress, MariaDB, Nginx, and PHP-FPM
  hosts: 10.48.148.168
  remote_user: root
  # remote_user: user
  # sudo: yes

  roles:
    - common
    - mariadb
    - nginx
    - php-fpm
    - wordpress
